<%= ajax_will_paginate @books, :class => "pager" %>
<div id="gallery">
  <ul id="grid">
    <% @books.each do |book| %>
     <li><%= link_to image_tag(book.image_link), book %></li>
    <% end %>
  </ul>
</div>
